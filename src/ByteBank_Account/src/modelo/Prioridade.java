package ByteBank_Account.src.modelo;

public enum Prioridade {
    /**
     * An enum type is a special data type that enables for a variable
     * to be a set of predefined constants
     * For example, you would specify a days-of-the-week enum type as:
     *
     *
     * public enum modelo.Day {
     *     SUNDAY, MONDAY, TUESDAY, WEDNESDAY,
     *     THURSDAY, FRIDAY, SATURDAY
     * }
     * You should use enum types any time you need to represent a fixed set of constant
     */


    MIN,NORMAL,MAX;
}
